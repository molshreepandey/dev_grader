"""Sequences assignment — the student replaces this file with their implementation.

Only this file is taken from the student's repository; everything else in the graded
workspace comes from the template.
"""


def fib(n: int) -> int:
    """Return the nth Fibonacci number, with fib(0) == 0 and fib(1) == 1."""
    raise NotImplementedError


def fib_sequence(n: int) -> list[int]:
    """Return the first n Fibonacci numbers, starting at fib(0)."""
    raise NotImplementedError


def sum_even_fibs(limit: int) -> int:
    """Return the sum of the even Fibonacci numbers that are strictly less than limit."""
    raise NotImplementedError
