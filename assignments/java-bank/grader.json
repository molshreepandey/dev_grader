{
  "stack": "java",
  "merge": {
    "mode": "whole_project",
    "protected_paths": ["src/test", "pom.xml"]
  },
  "install": ["mvn", "-B", "-q", "dependency:go-offline"],
  "test": ["mvn", "-o", "-q", "test"],
  "report": { "glob": "target/surefire-reports/*.xml" }
}
